# ============================================
# RAGSystem Requirements (完整安装)
# 版本: v3.0 (SQLite + sqlite-vec)
# 更新日期: 2026-01-24
# Python版本: 3.10+
# ============================================
#
# 重大更新：
# - 使用 SQLite + sqlite-vec 替代 ChromaDB
# - 使用远程 Embedding API 替代本地模型
# - 体积减少 ~500MB (移除 PyTorch/sentence-transformers)
#
# ============================================

# Flask核心框架
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7

# Neo4j数据库驱动
neo4j==6.0.3

# HTTP请求库
requests==2.32.5

# 环境变量管理
python-dotenv==1.0.0

# 配置管理
pydantic==2.12.5
PyYAML==6.0.3

# 日志处理
coloredlogs==15.0.1
structlog>=23.1.0

# 知识图谱构建依赖
python-docx==1.2.0
tiktoken==0.12.0
json-repair==0.54.3

# 向量数据库 (新版：SQLite + sqlite-vec)
# 零依赖部署，单文件数据库，支持向量检索
sqlite-vec>=0.1.0

# 中文分词（用于文本预处理）
jieba==0.42.1

# 数据处理
numpy>=1.26.0
pandas==2.3.3

# 空间数据处理，搭配neo4j使用
shapely==2.1.2

# 自定义依赖（需源码安装）
# llmjson: git+https://github.com/lihao77/llmjson.git
# json2graph: git+https://github.com/lihao77/json2graph-for-review.git
