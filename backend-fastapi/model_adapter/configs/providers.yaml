# Model Adapter Provider 配置示例（与当前实现一致）
# 使用说明：
# 1. 复制此文件为 providers.yaml（providers.yaml 已加入 .gitignore）
# 2. 填入真实 api_key，或使用 ${ENV_VAR} 从环境变量读取
# 顶层键为复合键：{name}_{provider_type}，如 my_deepseek、my_openrouter

test_deepseek:
  name: test
  provider_type: deepseek
  api_key: "sk-568644af224a4aac93ee2b5e10b9db90"
  api_endpoint: ""
  max_completion_tokens: 4096      # 单次输出的最大 token 数
  max_context_tokens: 128000       # 模型支持的最大上下文窗口（DeepSeek V3: 128K）
  temperature: 0.7
  timeout: 30
  retry_attempts: 3
  retry_delay: 1.0
  supports_function_calling: true
  model_map:
    chat: deepseek-chat
  models: []

test_openrouter:
  name: test
  provider_type: openrouter
  api_key: "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"
  api_endpoint: ""
  max_completion_tokens: 4096
  max_context_tokens: 128000       # 根据实际使用的模型配置（如 GPT-4 Turbo: 128K）
  temperature: 0.7
  timeout: 30
  retry_attempts: 3
  retry_delay: 1.0
  supports_function_calling: true
  model_map:
    chat: []
    embedding:
      - openai/text-embedding-3-small
    reasoning: []
  models:
    - openai/text-embedding-3-small
